name: Patch official aggregate and unit sources

on:
  push:
    branches:
      - fix/ims-official-aggregate-units
    paths:
      - .github/workflows/patch-official-aggregate-units.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/ims-official-aggregate-units
      - name: Apply dashboard official-layer patch
        run: |
          python tools/patch_dashboard_official_layers.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/query/dashboard_query.py
          git commit -m "fix: align dashboard global and region KPIs to official aggregates"
          git push origin HEAD:fix/ims-official-aggregate-units
