name: Verify Production Manager User

on:
  push:
    paths:
      - '.github/workflows/targets-latest-period-acceptance.yml'

permissions:
  contents: read

jobs:
  verify-manager:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    environment: production
    env:
      IMS_HOST: ${{ vars.IMS_SERVER_HOST || '130.162.48.162' }}
      IMS_PORT: ${{ vars.IMS_SERVER_PORT || '22' }}
      IMS_USER: ${{ vars.IMS_SERVER_USER || 'ubuntu' }}
      IMS_PATH: ${{ vars.IMS_DEPLOY_PATH || '/home/ubuntu/ims_system' }}
    steps:
      - name: Configure production SSH key
        shell: bash
        env:
          IMS_DEPLOY_SSH_KEY: ${{ secrets.IMS_DEPLOY_SSH_KEY }}
        run: |
          set -Eeuo pipefail
          mkdir -p ~/.ssh
          printf '%s\n' "$IMS_DEPLOY_SSH_KEY" > ~/.ssh/ims_diag_key
          chmod 600 ~/.ssh/ims_diag_key
          ssh-keyscan -p "$IMS_PORT" -H "$IMS_HOST" >> ~/.ssh/known_hosts

      - name: Verify manager account read only
        shell: bash
        run: |
          set -Eeuo pipefail
          ssh -o ServerAliveInterval=30 -o ServerAliveCountMax=6 -i ~/.ssh/ims_diag_key -p "$IMS_PORT" -o BatchMode=yes "$IMS_USER@$IMS_HOST" "IMS_PATH='$IMS_PATH' bash -se" <<'REMOTE'
          set -Eeuo pipefail
          cd "$IMS_PATH"
          PYTHONPATH="$IMS_PATH" venv/bin/python - <<'PY'
          from app import create_app
          from app.extensions import db
          from app.models import User
          from app.access_control import is_manager

          app=create_app()
          with app.app_context():
              active=int(db.session.execute(db.text("SELECT COUNT(*) FROM ims_import_jobs WHERE status IN ('PROCESSING','QUEUED')")).scalar() or 0)
              user=User.query.filter_by(email='mehmet.ozkocak@bilimilac.com').one_or_none()
              print(f"IMS_JOB_GATE|active={active}")
              print(f"USER|exists={int(user is not None)}|id={getattr(user,'id',None)}|name={getattr(user,'full_name',None)}|role={getattr(user,'role',None)}|active={getattr(user,'active',None)}|manager={int(is_manager(user)) if user else 0}")
              assert active==0
              assert user is not None
              assert user.full_name=='Mehmet Özkoçak'
              assert user.role=='Manager'
              assert user.active is True
              assert is_manager(user)
              assert User.query.filter_by(email='mehmet.ozkocak@bilimilac.com').count()==1
              admin=User.query.filter_by(email='admin@ipm.local').one()
              print(f"ADMIN|id={admin.id}|role={admin.role}|preserved=1")
              assert admin.role=='Admin'
              journal=db.session.execute(db.text('PRAGMA journal_mode')).scalar()
              busy=db.session.execute(db.text('PRAGMA busy_timeout')).scalar()
              print(f"SQLITE|journal={journal}|busy_timeout={busy}")
              assert str(journal).lower()=='wal' and int(busy)==30000
              print('MANAGER_USER_VERIFY|PASS')
          PY
          REMOTE
